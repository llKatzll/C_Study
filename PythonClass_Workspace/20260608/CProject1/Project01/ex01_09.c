#include <stdio.h>

int main() {

	//int는 4바이트.
	//double은 8바이트.

	//float은 4바이트.
	//char은 1바이트.

	//비트 : 가장 작은 단위. 바이너리 디짓 (binary digit. 이진 숫자)의 줄임말.
	// 
	//컴퓨터가 표현할 수 있는 아주 작은 단위. 0 과 1로 구성.
	// 
	//비트 1개로는 2가지(0, 1)밖에 표현 못 해. 근데 여러 개를 묶으면 표현할 수 있는 경우의 수가 늘어나:

	//1비트 → 2가지(2¹)
	//	2비트 → 4가지(00, 01, 10, 11 = 2²)
	//	3비트 → 8가지(2³)
	//	n비트 → 2ⁿ 가지

	//바이트 : 비트 8개를 묶은 단위. 컴퓨터가 데이터를 처리할때 보통 비트 단위가 아닌 바이트 단위로 처리함. 그래서 기본 단위를 말할 경우
	//보통은 바이트인 경우가 많음.

	//1바이트 == 8비트                                            <- 중요
	//고로 표현할 수 있는 값? 2의8승. 256가지 (0부터 255까지)

	//char   c;   // 1바이트 (8비트)
	//int    n;   // 보통 4바이트 (32비트)
	//double d;   // 8바이트 (64비트)

	//printf("%d\n", sizeof(char));    // 1
	//printf("%d\n", sizeof(int));     // 4
	//printf("%d\n", sizeof(double));  // 8

	//예를 들어 1바이트(char)는 256가지밖에 표현 못 하니까, 
	// 부호 있는 정수로 쓰면 -128 ~ 127까지밖에 못 담는다.
	// 그래서 큰 숫자를 다룰 땐 int(4바이트)를 쓰는 것.

	//char  small = 200;   // 위험! char 범위(-128~127) 초과 → 값이 깨짐
	//int   big = 200;   // 안전

	//비트(bit): 0 또는 1, 가장 작은 단위
	//바이트(byte) : 비트 8개 = 256가지 표현 가능
	//C에서 자료형 크기(char 1, int 4, double 8바이트)가 곧 표현 범위를 결정
	//비트가 많을수록 더 많은 값 / 더 큰 수를 담을 수 있음

	int bonbong, bonus, total;

	double tax, real_income;

	bonbong = 150;
	bonus = 60;

	total = bonbong + bonus;

	tax = total * 0.09f;

	real_income = total - tax;

	//정수는 %d로, 실수(double)은 %lf로 데려와야한다!
	printf("총 수입액 : %lf", real_income);

	return 0;
}